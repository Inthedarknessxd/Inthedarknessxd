function aes() {
let  num = window.prompt("Digite um Numero")
let numMenos = num - 1
let numMais = num+1
let rusultado = window.alert(`Antes de ${num}, temos o numero ${numMenos} /nDepois de ${num},temos o numero ${numMais}`)
}